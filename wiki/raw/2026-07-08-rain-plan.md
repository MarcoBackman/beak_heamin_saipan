---
project: shared
status: draft
---

# 우천 플랜 병기 — 날씨 적응형 일정

## 배경 및 목표

day별 날씨(예보/평년)에 따라 우천 시 대체 액티비티를 일정에 병기한다. 항공편·로타
경비행기·마나가하 페리 등 예약이 고정돼 있고 실예보는 출발 약 16일 전부터만 제공되므로,
일정 자체를 자동 변경하지 않고 **큐레이션된 우천 플랜을 나란히 보여주고 현장에서 두 사람이
결정**하는 방식을 택한다 (사용자 확정).

**목표**
1. D2~D7 각 day 일정 아래 '☔ 우천 시 플랜' 접이식 블록을 병기한다 (D1 심야 도착·D8 새벽 출국 제외).
2. 해당 day의 해석된 날씨(예보 또는 9월 평년)가 비/뇌우면 블록을 자동 펼침 + 시각 강조하고,
   day 헤더에 "☔ 우천 플랜 확인" 미니 칩을 띄운다.
3. JS 실패·인쇄·오프라인에서도 우천 플랜 콘텐츠에 접근 가능해야 한다.

**성공 기준**: 현재(평년 폴백) 기준 D3·D7(rain)·D5(thunder)가 자동 펼침+강조 상태로 보이고,
sun/cloud day는 접힌 기본 상태. JS 차단 시에도 수동 개폐 동작. 인쇄물에 우천 플랜 포함.
기존 기능 회귀 0.

## 선택한 접근과 이유

**정적 HTML 접이식 병기 블록 + weather.js 최소 연동** (2단계 객관식으로 확정)

- 콘텐츠는 index.html의 각 day에 `<details class="rain-plan">`로 정적 추가 — 일정 본문이
  index.html에 있는 기존 콘텐츠 소유 구조와 일치, JS 무관하게 동작.
- weather.js `renderDay()`에 소규모 추가 — 테마가 `rain|thunder`면 `.rain-plan` 자동 펼침 +
  ☔ 칩. 별도 신규 JS 파일 없음.
- 강조 스타일은 `article.day[data-wx=rain] .rain-plan` 등 CSS 셀렉터만으로 처리(배너 역할).

## 아키텍처

- **index.html** — D2~D7 `.sched` 아래(참고 콘텐츠 블록 위) `details.rain-plan` 추가:
  `<summary>☔ 우천 시 플랜</summary>` + `<ul>` 불릿 2~4개.
- **js/weather.js** — renderDay에서 theme이 rain/thunder일 때:
  ① `art.querySelector('.rain-plan')`이 있으면 `open = true`
  ② `.day-head`에 `.wx-rain-chip`("☔ 우천 플랜 확인") append. 부재 시 no-op.
- **css/weather.css** — `.rain-plan` 기본 스타일(기존 details.practical·fold와 시각 언어 통일),
  `[data-wx=rain] .rain-plan, [data-wx=thunder] .rain-plan` 강조(테두리·배경 틴트), `.wx-rain-chip`.
- **인쇄** — `window.addEventListener('beforeprint', ...)`로 `.rain-plan` 강제 펼침(weather.js에
  2줄 — printBtn과 브라우저 Ctrl+P 모두 커버). 우천 플랜은 오프라인·인쇄에서 가치가 크므로
  인쇄물에 포함한다.
- **캐시** — 수정 파일 index.html 내 weather.js/weather.css `?v=` 증가.

## 데이터 흐름

```
weather.js: 예보/평년 해석 → data-wx (기존)
          → theme rain|thunder → .rain-plan.open = true + ☔ 칩 append
CSS: data-wx 기반 .rain-plan 강조 (JS와 무관하게 동작)
사용자: 어느 날이든 details 수동 개폐 가능
```

## 인터페이스 (모듈 경계)

- 신규 JS API 없음. 계약은 두 가지뿐:
  - `data-wx` 속성 (기존 — weather.js 생산, weather.css 소비)
  - `.rain-plan` 클래스 (index.html 생산, weather.js·weather.css 소비)
- weather.js는 `.rain-plan` 부재 시 아무것도 하지 않음 (D1·D8 안전).

## 에러 처리

- weather.js 로드 실패 → 접이식 블록은 정적 HTML이라 그대로 사용 가능 (강조·자동 펼침만 없음).
- 예보 없음(현재~8월) → 평년 테마 기준 동일 동작.
- data.js 실패 → weather.js 가드로 종료(기존 패턴), 블록은 정적으로 잔존.

## 우천 플랜 콘텐츠 (확정 초안 — 사용자 승인됨)

공통 원칙(첫 번째 우천 블록 D2 상단에 1줄 반영): 비 자체는 수영·다이빙에 지장 적음(어차피
젖음) — 문제는 **번개(입수 금지), 파도(보트 결항), 진창(트레킹)**. 9월 사이판 스콜은 대개
30분~1시간이라 "대피 후 재개"가 1순위.

| Day | 우천 시 플랜 |
|---|---|
| D2 그로토·라우라우 | 스콜이면 순서 스왑(라우라우↔그로토) 후 재개. 번개면 입수 중단 → 아메리칸 메모리얼 파크 비지터센터(실내 전시), 조텐 마트 간식 장보기, 호텔 스파. 그로토 적기 게양 시 무조건 라우라우로 |
| D3 로타홀 보트 | 파도로 보트 취소 시 → 토가 동굴(실내 동굴, 우천 무관), 송송 빌리지 산책·카페, 피나탕 파크(스웰 없으면 비 와도 가능). 빛내림은 D4 오전 재시도 여지(업체 협의) |
| D4 테테토·스위밍홀 | 우천 시 스위밍홀 비포장 진입로 진창 → 스킵하고 테테토만(번개만 아니면 수영 가능). 남는 시간 토가 동굴·송송 카페. 오후 비행이라 무리하지 않기 |
| D5 마나가하 | 페리는 웬만한 비엔 운행, 결항 시 → 가라판 데이로 전환: 아이러브사이판 쇼핑(D7 앞당기기), 카페 투어, 호텔 스파. 섬에서 스콜은 파빌리온 대피 후 재개 (※ 메모리얼 파크 비지터센터는 월 휴관이라 제외) |
| D6 포비든 트레킹 | 우천 시 투어 취소가 보통(진창·미끄럼) → D7 윙 비치와 통째로 스왑 시도, 또는 마이크로 비치(라군은 비에도 잔잔) + 쇼핑 선행 |
| D7 윙비치·가라판 | 윙 비치 옵션 스킵(파도 있는 날 원래 금지). 쇼핑·디너는 우천 무관. 선셋 크루즈 취소 시 가라판 디너 연장 |

## 테스트 전략

브라우저 실동작 검증 (기존 컨벤션 — 테스트 프레임워크 없는 정적 페이지):

1. **390px/1280px** — D2~D7 블록 렌더, 평년 폴백 기준 D3·D5·D7 자동 펼침+강조+☔ 칩,
   sun/cloud day 접힘·비강조, 콘솔 에러 0
2. **JS 차단** — weather.js 미로드 상태에서 블록 수동 개폐 동작
3. **인쇄 미리보기** — 우천 플랜 내용 포함(강제 펼침), 날씨 연출 레이어는 기존대로 숨김
4. **회귀** — 배지·배경 연출·지도 오버레이·접힘(fold) 무영향, ResizeObserver와 상호작용
   (블록 펼침 → 카드 높이 변화 → 캔버스 추적) 정상
5. **배포** — Pages 실URL 확인

## 리스크

- 🟡 콘텐츠 사실성 — 토가 동굴 상시 개방 여부, 아메리칸 메모리얼 파크 비지터센터 운영시간
  등은 구현 단계에서 웹 검색으로 확인 후 확정. 불확실하면 문구를 보수적으로("~가능 여부 현지 확인")
- 🟡 로타 경비행기 악천후 지연 — 일정 변경이 아닌 안내 문구로만 반영(예약 고정)
- 🟢 회귀 — 순수 추가 변경. 단 rain-plan 펼침이 카드 높이를 바꾸므로 날씨 캔버스가 따라와야
  함 — 직전 수정(ResizeObserver)으로 커버됨, 테스트 4에서 확인

## 기각된 접근

- **예보 기반 자동 스왑** — 예보 불확실성·예약 고정과 충돌, 원래 일정이 숨겨져 혼란 → 기각
- **토글 스왑(기본↔우천 교체)** — 두 플랜 동시 비교 불가, 우천판 전체 일정 별도 작성 부담 → 기각
- **별도 '우천 대비' 통합 섹션** — 해당 날짜를 볼 때 왔다갔다 필요 → 기각
- **data.js 데이터 주도 렌더** — 일정 본문이 index.html에 있는 기존 소유 구조와 불일치,
  이동 파트가 늘어남 → 기각 (YAGNI)

## 콘텐츠 사실 확인 (2026-07-08 웹 검증)

- **토가 동굴(Tonga Cave)** — 송송 빌리지 인근 대형 석회 동굴, 방문 가능한 역사 유적
  (로타 시장실 historic sites 목록·마리아나 관광청 확인). 태풍 대피처로 쓰였을 만큼 실내 안전.
- **아메리칸 메모리얼 파크 비지터센터** — NPS 공식: **화·목·금·토 09:00~16:00 개관**.
  여행 중 D2(금)·D6(화)는 개관일, **D5(월)는 휴관** → D5 대체안에서 제외하고 D2·D6에만 포함.
- 조텐(Joeten) 마트·아이러브사이판 — 상시 영업 체인, 별도 확인 불요.

## 구현 계획

**목표:** D2~D7 일정에 '☔ 우천 시 플랜' 접이식 블록을 병기하고, 비/뇌우인 날은 자동 펼침·강조·☔ 칩으로 안내한다.

**아키텍처:** index.html의 각 day `.sched` 직후에 `details.rain-plan` 정적 블록 추가(6곳, 균일 앵커). weather.css에 기본·강조(`[data-wx=rain|thunder]`) 스타일 추가. weather.js `renderDay()`에서 rain/thunder면 자동 펼침 + `.wx-rain-chip` 주입, `beforeprint`/`afterprint`로 인쇄 시 강제 펼침·복원.

**기술 스택:** 정적 HTML(`<details>`), CSS, Vanilla JS(기존 weather.js IIFE 내 추가).

### 전역 제약 (Global Constraints)

- 빌드 도구·npm 의존성 도입 금지 — 정적 파일만
- ES 모듈 금지 — `<script defer>` 클래식만
- 데스크톱·기존 기능 회귀 금지 (순수 추가 변경)
- 수정 파일은 index.html `?v=` 증가 (weather.css v=3→4, weather.js v=3→4)
- 커밋 메시지는 기존 컨벤션(짧은 영어 명령형, 프리픽스 없음), commit 스킬로 커밋

---

## 태스크

### Task 1: rain-plan-blocks — 정적 블록 + 스타일

**파일:**
- 수정: `index.html` — D2(`</ul>` 286행 직후)·D3(326)·D4(358)·D5(383)·D6(409)·D7(434) 각 `.sched` 닫는 태그 직후 + weather.css `?v=4`
- 수정: `css/weather.css` — 파일 끝에 append

**인터페이스:**
- 생산: `details.rain-plan` DOM (D2~D7) — Task 2(weather.js)가 소비. D1·D8에는 없음(소비자는 부재 시 no-op이어야 함)

- [ ] **Step 1: index.html에 6개 블록 추가** — 각 day `.sched`의 `</ul>` 바로 다음 줄에 삽입:

D2 (금):
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>비 자체는 다이빙에 지장 없음(어차피 젖어요) — 문제는 <b>번개·파도·진창</b>. 9월 스콜은 대개 30분~1시간이니 '대피 후 재개'가 1순위 ⏱️</li>
    <li>스콜이면 순서 스왑: 라우라우 ↔ 그로토 — 갠 쪽부터 재개</li>
    <li>번개면 입수 중단 → 아메리칸 메모리얼 파크 비지터센터(실내 전시 · 화목금토 09~16시), 조텐 마트 간식 장보기, 호텔 스파</li>
    <li>그로토 적기(빨간 깃발) 게양 시 무조건 라우라우로 — 당일 아침 가이드 판단 우선</li>
  </ul>
</details>
```

D3:
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>파도로 로타 홀 보트 취소 시 → 토가 동굴(송송 인근 대형 석회 동굴 · 우천 무관), 송송 빌리지 산책·카페</li>
    <li>피나탕 파크는 스웰만 없으면 비 와도 가능 — 바깥 수로만 금지</li>
    <li>빛내림은 내일(D4) 오전 재시도 여지 — 업체와 미리 협의해두기</li>
    <li>경비행기는 악천후 시 지연될 수 있음 — 오전 일정에 여유 두기 🛩️</li>
  </ul>
</details>
```

D4:
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>우천 시 스위밍 홀 비포장 진입로가 진창 → 과감히 스킵, 테테토 위주로(번개만 아니면 수영 OK)</li>
    <li>남는 시간엔 토가 동굴·송송 카페 — 오후 비행이라 무리하지 않기</li>
  </ul>
</details>
```

D5 (월 — 메모리얼 파크 비지터센터 휴관일이라 제외):
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>페리는 웬만한 비엔 운행 — 파도로 결항 시 가라판 데이로 전환: 아이러브사이판 쇼핑(내일 몫 앞당기기), 카페 투어, 호텔 스파</li>
    <li>섬에서 스콜을 만나면 파빌리온 대피 후 재개 — 짐은 방수백에</li>
    <li>번개 예보면 리프 에지 프리다이빙은 포기, 라군 스노클만 짧게</li>
  </ul>
</details>
```

D6 (화):
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>우천 시 트레킹 투어는 보통 취소(진창·로프 구간 미끄럼) — 무리 금지</li>
    <li>1안: 내일(D7) 윙 비치 옵션과 통째로 스왑 — 포비든은 내일 썰물 시간 확인</li>
    <li>2안: 마이크로 비치(라군은 비에도 잔잔) + 아메리칸 메모리얼 파크 비지터센터(화 개관) + 쇼핑 선행</li>
  </ul>
</details>
```

D7:
```html
<details class="rain-plan">
  <summary>☔ 우천 시 플랜</summary>
  <ul>
    <li>윙 비치 옵션은 스킵 — 파도 있는 날은 원래 입수 금지 포인트</li>
    <li>쇼핑·디너는 우천 무관 — 일정 그대로</li>
    <li>선셋 크루즈 취소 시 가라판 레스토랑 디너를 여유 있게 연장 🥂</li>
  </ul>
</details>
```

- [ ] **Step 2: css/weather.css 끝에 스타일 append**

```css
  /* ── 우천 시 플랜 ─────────────────── */
  .rain-plan{
    margin:14px 0 0;border:1px dashed var(--border-strong);border-radius:10px;
    background:rgba(252,251,248,.6);
  }
  .rain-plan summary{
    cursor:pointer;padding:11px 14px;font-size:14px;font-weight:600;
    list-style:none;display:flex;align-items:center;gap:8px;
  }
  .rain-plan summary::-webkit-details-marker{display:none}
  .rain-plan summary::after{content:"▾";margin-left:auto;color:var(--muted);transition:transform .2s}
  .rain-plan[open] summary::after{transform:rotate(180deg)}
  .rain-plan ul{margin:0;padding:0 16px 12px 32px;font-size:14px;line-height:1.6}
  .rain-plan li{margin:4px 0}
  /* 비/뇌우인 날 강조 (배너 역할 — CSS만으로 동작) */
  article.day[data-wx=rain] .rain-plan,
  article.day[data-wx=thunder] .rain-plan{
    border-style:solid;border-color:#7d95ad;
    background:rgba(100,125,150,.10);
  }
```

- [ ] **Step 3: index.html weather.css `?v=4`**

- [ ] **Step 4: 검증**
실행: 로컬 서버 390px·1280px
기대: D2~D7에 접힌 '☔ 우천 시 플랜' 블록(D1·D8 없음), 수동 개폐 동작, 평년 폴백 기준 D3·D5·D7 블록이 파란 톤 강조(아직 자동 펼침은 안 됨 — Task 2), 콘솔 에러 0. weather.js `<script>` 태그를 임시 주석 처리해도 블록 개폐 정상(정적 동작 확인 후 원복)

- [ ] **Step 5: 커밋** — 예: `Add rain-day alternate plan blocks`

### Task 2: wx-autoopen-hook — 자동 펼침 + ☔ 칩 + 인쇄

**파일:**
- 수정: `js/weather.js` — renderDay 끝부분 + IIFE 하단
- 수정: `css/weather.css` — `.wx-rain-chip` append
- 수정: `index.html` — weather.js `?v=4`

**인터페이스:**
- 소비: Task 1의 `details.rain-plan` (부재 시 no-op), 기존 `wx.theme`·`.day-head`
- 생산: `.wx-rain-chip` DOM — weather.css만 소비

- [ ] **Step 1: weather.js renderDay에 추가** — `if (S.initWxVisual) ...` 줄 직전에:

```js
    if (wx.theme === 'rain' || wx.theme === 'thunder'){
      const rp = art.querySelector('.rain-plan');
      if (rp){
        rp.open = true;
        if (head){
          const c = document.createElement('span');
          c.className = 'wx-rain-chip';
          c.textContent = '☔ 우천 플랜 확인';
          head.appendChild(c);
        }
      }
    }
```

- [ ] **Step 2: weather.js IIFE 하단(getForecast().then 아래)에 인쇄 훅 추가**

```js
  /* 인쇄 시 우천 플랜 강제 펼침 → 인쇄 후 원상 복구 */
  let printOpened = [];
  window.addEventListener('beforeprint', () => {
    printOpened = Array.prototype.slice.call(document.querySelectorAll('.rain-plan:not([open])'));
    printOpened.forEach(d => { d.open = true; });
  });
  window.addEventListener('afterprint', () => {
    printOpened.forEach(d => { d.open = false; });
    printOpened = [];
  });
```

- [ ] **Step 3: css/weather.css에 칩 스타일 append**

```css
  .wx-rain-chip{
    display:inline-flex;align-items:center;gap:4px;
    font-size:12px;font-weight:600;padding:3px 10px;border-radius:9999px;
    background:rgba(100,125,150,.16);color:#3f566d;
    border:1px solid #7d95ad;
  }
```

- [ ] **Step 4: index.html weather.js `?v=4`**

- [ ] **Step 5: 검증**
실행: 로컬 서버 390px·1280px
기대: 평년 폴백 기준 D3·D5·D7 블록 자동 펼침 + day-head에 '☔ 우천 플랜 확인' 칩, sun/cloud day(D1·D2·D4·D6·D8 상당)는 접힘·칩 없음, 블록 펼침으로 카드가 커져도 비 캔버스가 하단까지 추적(ResizeObserver 회귀 확인), 인쇄 미리보기에 모든 우천 플랜 내용 표시 + 미리보기 닫으면 원상 복구, 콘솔 에러 0

- [ ] **Step 6: 커밋** — 예: `Auto-open rain plans on rainy forecast days`

### Task 3: rainplan-verify — 통합 검증 + 배포

**파일:** 없음 (검증 전용)

- [ ] **Step 1: 통합 검증**
실행: 로컬 서버 — 테스트 전략 1~4 전체 (390px/1280px, JS 차단, 인쇄, 회귀), `file://` 스모크
기대: 설계 문서 '테스트 전략' 5항목 중 1~4 전부 통과

- [ ] **Step 2: push + Pages 배포 확인**
실행: `git push` → GitHub Actions 2종 성공 확인 → 배포 URL 데스크톱·모바일 확인
기대: 404 없음, 우천 플랜·자동 펼침·칩 정상, 기존 기능 회귀 없음

=== 이슈 트래커 태스크-이슈 매핑 ===
project: shared
parentIssue: (없음)
taskIssues: (없음)
